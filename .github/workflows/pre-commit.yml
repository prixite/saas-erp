name: Pre-commit
on:
  pull_request:
    branches: 'master'
jobs:
  pre-commit:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - uses: pre-commit/action@v3.0.0
        - name: Run Pre-Commit
          run: pre-commit run --all
